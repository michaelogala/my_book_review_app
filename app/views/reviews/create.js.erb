let review = document.createRange()
                       .createContextualFragment("<%= j(render @review) %>")

document.getElementById('reviews').appendChild(review)

document.querySelector("textarea#review_comment").value = ""

let bookDetailsDiv = document.getElementById('book-details')
bookDetailsDiv.innerHTML = "<%= j(render @book) %>"
